let canvas = document.getElementById('canvas');
let ctx = canvas.getContext('2d');
let scrn = canvas;

const PIXSIZE = 8;
const WIDTH = canvas.width / PIXSIZE;
const HEIGHT = canvas.height / PIXSIZE;
